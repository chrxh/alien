1, comp, token: 16000
2, comp
mov SCANNER_INOUT_CELL_NUMBER, 0
mov CONSTR_IN, CONSTR_IN::SAFE
mov ENERGY_GUIDANCE_IN, ENERGY_GUIDANCE_IN::BALANCE_TOKEN
mov ENERGY_GUIDANCE_IN_VALUE_TOKEN, 200
mov ENERGY_GUIDANCE_IN_VALUE_CELL, 150
mov CONSTR_INOUT_ANGLE, 0xD0
mov i,100
mov BRANCH_NUMBER, 4
4, prop
1, comp
5, prop
4, prop
3, weap
2, comp
if i > 0
  sub i,1
  mov BRANCH_NUMBER, 0
endif
3, constr
1, comp
mov BRANCH_NUMBER,2
0, weap
5, weap
3, weap
5, comp
mov PROP_IN_POWER, 0x80
mov PROP_IN, PROP_IN::TOWARD_CENTER
sub i,1
if i > 0
  mov BRANCH_NUMBER, 3
  mov PROP_IN_POWER, 0xF0
  mov PROP_IN,PROP_IN::BY_ANGLE
  mov PROP_IN_ANGLE, 0xD0
endif
if i > 30
  mov PROP_IN_ANGLE, 127
endif
if i > 90
  mov PROP_IN, PROP_IN::DAMP_ROTATION
endif
3, comp
1, comp
mov CONSTR_IN_OPTION, CONSTR_IN_OPTION::STANDARD
if SCANNER_OUT = SCANNER_OUT::FINISHED
  mov CONSTR_IN_OPTION, CONSTR_IN_OPTION::FINISH_WITH_TOKEN_SEP_RED
endif
if SCANNER_OUT = SCANNER_OUT::RESTART
  mov CONSTR_INOUT_ANGLE,0xD0
  mov i,100
  mov SCANNER_OUT, SCANNER_OUT::SUCCESS
endif
mov CONSTR_IN_CELL_MAX_CONNECTIONS,CONSTR_IN_CELL_MAX_CONNECTIONS::AUTO
4, comp
mov BRANCH_NUMBER, 5
if CONSTR_OUT != CONSTR_OUT::SUCCESS
  mov BRANCH_NUMBER, 3
endif
4, prop
0, scan