The package boost-any is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS any)
    target_link_libraries(main PRIVATE Boost::any)

or the generated cmake configs via:

    find_package(boost_any REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::any)
