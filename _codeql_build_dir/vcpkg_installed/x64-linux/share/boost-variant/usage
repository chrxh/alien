The package boost-variant is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS variant)
    target_link_libraries(main PRIVATE Boost::variant)

or the generated cmake configs via:

    find_package(boost_variant REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::variant)
