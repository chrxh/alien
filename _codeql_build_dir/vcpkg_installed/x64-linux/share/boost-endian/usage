The package boost-endian is compatible with built-in CMake targets of FindBoost.cmake:

    find_package(Boost REQUIRED COMPONENTS endian)
    target_link_libraries(main PRIVATE Boost::endian)

or the generated cmake configs via:

    find_package(boost_endian REQUIRED CONFIG)
    target_link_libraries(main PRIVATE Boost::endian)
